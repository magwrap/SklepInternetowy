import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  text: {
    fontSize: 15,
    padding: 10,
  },
  container: {
    width: '100%',
    borderBottomWidth: 1,
    flexDirection: 'row',
    justifyContent: 'space-around',
  },
});
